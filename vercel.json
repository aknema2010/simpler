{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'no install needed — dependencies are vendored in /vendor'",
  "buildCommand": "echo 'no build needed — app.js is pre-compiled and committed'",
  "outputDirectory": "."
}
